<ul class="thumbnails" >
  <div id="main">
    <% unless designs.empty? %>
    <% designs.all.each do |design| %>
      <li class="item">
    <div class="thumbnail">
      <%= link_to design_path(design),class:"design" do %>
        <%= image_tag("#{design.image.thumb}",:alt => "#{design.title}", class:"img-rounded") %>
    <% end %>
      <p class="designListTitle"> <%= "#{design.title}" %></p>
  <% end %>
    </div>
    </li>
  <% end %>
  </div>
</ul>
