  <div class="row">

      <div class="span12">

      <div class="box">
        <h4>按照分类目录筛选</h4>
        <% @categorys.each do |category| %>
          <%= link_to category,class:"badge badge-success designTag" do %>
            <i class="icon-folder-open icon-white"></i>   <%= "#{category.name}" %>
          <% end %>
        <% end %>
        <%= link_to 'categorys/others',class:"badge badge-success designTag" do %>
          <i class="icon-folder-open icon-white"></i>   <%= "其它设计" %>
        <% end %>
      </div>


      <div class="box">
        <h4>按照标签筛选</h4>
        <% @tags.each do |tag| %>
          <%= link_to tag,class:"badge badge-success designTag" do %>
            <i class="icon-tag icon-white"></i>   <%= "#{tag.name}" %>
          <% end %>
        <% end %>
        <%= link_to 'tags/others',class:"badge badge-success designTag" do %>
          <i class="icon-tag icon-white"></i>   其它标签
        <% end %>
      </div>

        <div class="">
        <h3>最新上传的设计</h3>
        <%= render partial:"shared/content",locals:{designs:@designs} %>
        <%= paginate @designs, :theme => 'twitter-bootstrap' %>
      </div>
    </div> <!-- end of div class="span12" -->

  </div>
  <%= render "shared/flow_script" %>

